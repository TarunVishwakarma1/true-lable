<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>NSAppTransportSecurity</key>
	<dict>
		<!-- Local-network dev/testbed backends are typically plain HTTP on a
		     LAN IP, not HTTPS. NSAllowsLocalNetworking exempts only local
		     network addresses (192.168.x.x, 10.x.x.x, .local hostnames) from
		     ATS — arbitrary internet HTTP is still blocked. -->
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>
</dict>
</plist>
