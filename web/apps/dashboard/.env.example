# Same database the Rust backend connects to.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres

# Where this dashboard sends browser requests.
NEXT_PUBLIC_API_URL=http://localhost:8080
