# Template only — NOT applied by kustomization.yaml. See backend/secret.example.yaml
# for how the real `postgres-secrets` object gets created at deploy time.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secrets
type: Opaque
stringData:
  POSTGRES_USER: "postgres"
  POSTGRES_PASSWORD: "CHANGE_ME"
  POSTGRES_DB: "truelabel"
