apiVersion: v1
kind: Service
metadata:
  name: db-service
  labels:
    app: db-true-lable
spec:
  type: NodePort
  selector:
    app: db-true-lable
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
    nodePort: 30543
